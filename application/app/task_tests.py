from task_functions import *

### Use the assert statements to check that your code returns the expected value and is of a correct type.
# Use it especially to test the edge cases
# (e.g., when to return -1 or 0, what to do when the input is empty).

### Test the valid inputs ###
# arg1 = stopping_volume
# arg2 = signal_1
# result = trigger_accumulation(stopping_volume, signal_1)
# expected_result = "zero"
# print(f"--> trigger_accumulation({arg1, arg2}) returned `{result}`\n")
# assert result == expected_result